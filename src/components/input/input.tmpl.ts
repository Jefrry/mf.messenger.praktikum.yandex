const template: string = `<input type="{{type}}" class="form__field" placeholder="{{placeholder}}" name="{{name}}" />
                        <label for="{{name}}" class="form__label">{{placeholder}}</label>
                        {{icon}}`;

export {template};
